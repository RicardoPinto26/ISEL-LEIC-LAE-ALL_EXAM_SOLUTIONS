B > C > A

A tem 0 backing fields. Isto acontece devido ao getter ser declarado sem utilizar o campo field, e por não existir um
setter (val nunca gera setter).

B tem 2 backing fields.

C tem 1 backing field.